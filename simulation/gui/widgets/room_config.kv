#:kivy 1.11.1

# currently only shows information about environment variables
<RoomConfigWindow>:
    orientation: 'vertical'
    temperature: ""
    Label:
        size_hint_y: None
        height: dp(75)
        markup: True
        text: "[size=25][b]Temperature is {0}[/b][/size]".format(root.temperature)
    Label:
